plugins {
    kotlin("jvm")                                  // БЕЗ версии!
    id("org.jetbrains.compose")                    // версия задана в корне
    id("org.jetbrains.kotlin.plugin.compose")      // обязателен для Kotlin 2.x
}

// repositories { … } не нужны, уже в settings.gradle.kts

dependencies {
    implementation(compose.desktop.currentOs)
    implementation(project(":utils"))
    implementation("org.jetbrains.kotlinx:kotlinx-coroutines-core:1.8.1")
}

compose.desktop {
    application {
        mainClass = "MainWindowKt"
    }
}
